{
  "name": "office-addins",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "bun install",
    "build": "bun workspace run build",
    "test": "bun workspace run test",
    "lint": "bun workspace run lint"
  }
}