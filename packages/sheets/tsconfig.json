{
    "compilerOptions": {
        "module": "none", // don’t emit ES modules
        "target": "es6", // Apps Script V8+
        "outFile": "build/code.js", // concatenate into one file
        "rootDir": "src", // where your .ts files live
        "strict": true,
        "sourceMap": false,
        "types": ["google-apps-script"]
    },
    "include": ["src/**/*.ts"],
    "exclude": ["node_modules", "dist"]
}
